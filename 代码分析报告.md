# med_agent_v1 项目代码分析报告

> 分析日期：2026-06-29
> 项目仓库：https://github.com/Dillon-Xue/med_agent_v1

---

## 📋 项目概览

这是一个基于 **FastAPI + LangGraph + RAG** 的医疗智能体系统，功能涵盖：

- 多科室 Agent 路由（心内科、药剂科、全科）
- 多工具协同（药品、指南、文献、风险、患者档案等）
- 患者信息管理与加密存储
- 审批流管理
- 飞书适配层
- Prometheus 监控

### 技术栈

| 类别 | 技术 |
|------|------|
| Web 框架 | FastAPI + Uvicorn |
| Agent 框架 | LangGraph + LangChain |
| LLM | 通义千问 (DashScope) / Ollama |
| 向量数据库 | ChromaDB |
| 关系数据库 | MySQL |
| 部署方式 | Docker Compose |
| 监控 | Prometheus |
| 集成平台 | 飞书 |

### 项目结构

```
med_agent_v1/
├── agents/              # Agent 核心层
│   ├── supervisor.py    # 主管 Agent（科室路由）
│   ├── planner.py       # 规划器（工具选择）
│   ├── executor.py      # 执行器（并行调用工具）
│   ├── synthesizer.py   # 合成器（结果汇总）
│   ├── aggregator.py    # 多 Agent 结果聚合
│   ├── consult_graph.py # 问诊图（LangGraph）
│   └── ...
├── tools/               # 工具层
│   ├── drug_tool.py     # 药品工具
│   ├── guideline_tool.py # 指南工具
│   ├── literature_tool.py # 文献工具
│   ├── risk_tool.py     # 风险工具
│   ├── patient_tool.py  # 患者档案工具
│   ├── approval_tool.py # 审批工具
│   ├── report_tool.py   # 报告工具
│   ├── file_tool.py     # 文件解析工具
│   ├── memory_tool.py   # 记忆工具
│   └── ...
├── utils/               # 工具函数
│   ├── config.py        # 配置管理
│   ├── crypto.py        # 加密解密
│   ├── audit.py         # 审计日志
│   ├── embeddings.py    # 向量嵌入
│   └── response.py      # 响应封装
├── tests/               # 测试
├── chat.py              # 主应用（1300+ 行）
├── app.py               # 简化版 RAG 接口
├── feishu_adapter.py    # 飞书适配层
├── docker-compose.yml   # Docker 编排
├── init.sql             # 数据库初始化
└── requirements.txt     # 依赖
```

---

## 🔴 一、高危安全问题（P0 - 必须修复）

### 1. crypto.py 启动崩溃 bug

**严重程度**：🔴 致命

**问题描述**：
`utils/crypto.py` 中使用了 `logger.warning()`，但文件顶部没有导入 `logging` 也没有定义 `logger`。当 `CRYPTO_KEY` 环境变量为空时，程序启动会直接崩溃报 `NameError`。

**问题代码**：
```python
# utils/crypto.py 第1-10行
from cryptography.fernet import Fernet
import os

CRYPTO_KEY = os.getenv("CRYPTO_KEY")
if not CRYPTO_KEY:
    CRYPTO_KEY = Fernet.generate_key().decode()
    logger.warning(f"⚠️ 请将 CRYPTO_KEY={CRYPTO_KEY} 添加到 .env")  # logger 未定义！
    os.environ["CRYPTO_KEY"] = CRYPTO_KEY
```

**涉及文件**：[crypto.py](file:///workspace/med_agent_v1/utils/crypto.py#L9-L9)

**修复建议**：
```python
import logging
logger = logging.getLogger(__name__)
```

**隐蔽性**：如果配置了 `CRYPTO_KEY` 就不会触发，测试时容易忽略。

---

### 2. 加密密钥自动生成（数据丢失风险）

**严重程度**：🔴 高危

**问题描述**：
如果没有 `CRYPTO_KEY` 环境变量，系统会自动生成一个新密钥。这导致：

- 每次重启都会生成新密钥，**之前加密的患者数据全部无法解密**
- 多实例部署时，每个实例密钥不同，数据无法互通
- 容器重启后数据永久丢失

**问题代码**：
```python
# utils/crypto.py 第6-10行
if not CRYPTO_KEY:
    CRYPTO_KEY = Fernet.generate_key().decode()
    logger.warning(...)
    os.environ["CRYPTO_KEY"] = CRYPTO_KEY
```

**涉及文件**：[crypto.py](file:///workspace/med_agent_v1/utils/crypto.py#L6-L10)

**修复建议**：
强制要求配置 `CRYPTO_KEY`，没有就直接报错启动失败：
```python
CRYPTO_KEY = os.getenv("CRYPTO_KEY")
if not CRYPTO_KEY:
    raise RuntimeError("CRYPTO_KEY 环境变量必须设置，否则加密数据无法解密！")
```

---

### 3. 身份认证严重缺失（全局变量存用户）

**严重程度**：🔴 高危

**问题描述**：
使用全局变量 `current_session_user` 存储用户身份，完全不可靠：

- **并发安全问题**：多用户/多请求下会互相覆盖，A医生可能看到B医生的患者数据
- **无法横向扩展**：多进程/多实例部署时状态不一致
- **伪造身份**：任何人都可以通过说"用户：xxx"来冒充他人
- **无权限校验**：知道患者姓名就能查，没有数据权限控制

**全局引用**：7 个文件中共 51 处引用 `current_session_user`

**涉及文件**：
- [chat.py](file:///workspace/med_agent_v1/chat.py#L172-L172)
- [patient_tool.py](file:///workspace/med_agent_v1/tools/patient_tool.py#L90-L97)
- [approval_tool.py](file:///workspace/med_agent_v1/tools/approval_tool.py)
- 等 7 个文件

**修复建议**：
- 使用 JWT Token 或 Session 机制
- 从请求头中解析用户身份（如 `Authorization: Bearer xxx`）
- 工具调用时通过参数传递用户身份，而不是全局变量
- 增加数据权限校验（只能访问自己的患者数据）

---

### 4. 路径遍历漏洞（文件下载/预览）

**严重程度**：🔴 高危

**问题描述**：
报告下载和预览接口直接拼接用户输入的文件名，没有做路径校验。

**问题代码**：
```python
# chat.py 第965-973行
@app.get("/reports/{filename}")
async def download_report(filename: str):
    file_path = f"reports/{filename}"  # 危险！
    if not os.path.exists(file_path):
        return {"error": "文件不存在"}
    return FileResponse(file_path, ...)
```

**攻击方式**：
- 访问 `/reports/../etc/passwd` 可读取系统敏感文件
- 访问 `/reports/../.env` 可泄露数据库密码、API Key 等

**涉及文件**：
- [download_report](file:///workspace/med_agent_v1/chat.py#L965-L973)
- [preview_report](file:///workspace/med_agent_v1/chat.py#L1252-L1259)

**修复建议**：
```python
from pathlib import Path

REPORTS_DIR = Path("reports").resolve()

@app.get("/reports/{filename}")
async def download_report(filename: str):
    # 只取文件名部分，防止路径遍历
    safe_filename = Path(filename).name
    file_path = (REPORTS_DIR / safe_filename).resolve()
    
    # 确保最终路径在 reports 目录内
    if not str(file_path).startswith(str(REPORTS_DIR)):
        return {"error": "非法路径"}
    
    if not file_path.exists():
        return {"error": "文件不存在"}
    
    return FileResponse(str(file_path), ...)
```

---

### 5. CORS 配置过于宽松

**严重程度**：🔴 高危

**问题描述**：
允许所有来源跨域访问，且同时允许凭证，存在 CSRF 风险。

**问题代码**：
```python
# chat.py 第141-147行
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],          # 允许所有来源
    allow_credentials=True,       # 允许携带凭证
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**涉及文件**：[chat.py](file:///workspace/med_agent_v1/chat.py#L141-L147)

**安全风险**：
- `allow_origins=["*"]` 与 `allow_credentials=True` 不能同时使用（浏览器会拒绝）
- 恶意网站可以跨域调用接口获取患者数据

**修复建议**：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://yourdomain.com",  # 生产域名
        "http://localhost:3000",   # 开发环境
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

### 6. 敏感数据加密不完整

**严重程度**：🔴 高危（合规风险）

**问题描述**：
只有身份证号和手机号加密了，其他患者敏感信息明文存储：

| 字段 | 是否加密 |
|------|---------|
| 身份证号 | ✅ 加密 |
| 手机号 | ✅ 加密 |
| 姓名 | ❌ 明文 |
| 家庭住址 | ❌ 明文 |
| 过敏史 | ❌ 明文 |
| 用药史 | ❌ 明文 |
| 症状描述 | ❌ 明文 |
| 临床诊断 | ❌ 明文 |

**问题代码**：
```python
# tools/patient_tool.py 第240-241行
id_card_encrypted = encrypt_if_needed(id_card) if id_card else None
phone_encrypted = encrypt_if_needed(phone) if phone else None
```

**涉及文件**：[patient_tool.py](file:///workspace/med_agent_v1/tools/patient_tool.py#L240-L241)

**合规风险**：
根据《个人信息保护法》和医疗数据安全规范：
- 患者健康信息属于敏感个人信息
- 医疗数据需加密存储
- 需有访问控制和审计日志

**修复建议**：所有患者敏感字段全部加密存储，或使用数据库透明加密（TDE）。

---

## 🟠 二、架构与可靠性问题（P1 - 建议修复）

### 7. 循环依赖问题

**严重程度**：🟠 中危

**问题描述**：
模块间存在循环导入，通过动态 import 绕开了，但架构不健康。

```
chat.py → tool_registry.py → patient_tool.py
    ↑                                    ↓
    └────── 动态 import chat 获取 tenant/user ────┘
```

**问题代码**：
```python
# tools/patient_tool.py 第80-97行
def _get_tenant(self):
    try:
        import chat  # 循环依赖！
        if hasattr(chat, 'get_current_tenant'):
            return chat.get_current_tenant()
    except ImportError:
        pass
    return "default"
```

**涉及文件**：
- [patient_tool.py](file:///workspace/med_agent_v1/tools/patient_tool.py#L80-L97)
- [approval_tool.py](file:///workspace/med_agent_v1/tools/approval_tool.py)

**修复建议**：
使用依赖注入，将 tenant_id 和 user_id 作为参数传入工具的 `run` 方法：
```python
class BaseTool:
    def run(self, query: str, tenant_id: str, user_id: str) -> dict:
        ...
```

---

### 8. 无数据库连接池

**严重程度**：🟠 中危

**问题描述**：
每次数据库操作都新建连接，性能差且可能耗尽连接数。6个文件中都有各自的 `pymysql.connect` 调用。

**涉及文件**：
- `utils/audit.py`
- `tools/patient_tool.py`
- `tools/approval_tool.py`
- `tests/conftest.py`
- `chat.py`

**问题影响**：
- 高并发下连接数爆炸
- 频繁建连/断连性能差
- 可能出现"Too many connections"错误

**修复建议**：
使用连接池（推荐 SQLAlchemy + DBUtils）：
```python
from dbutils.pooled_db import PooledDB
import pymysql

pool = PooledDB(
    creator=pymysql,
    maxconnections=10,
    mincached=2,
    host=...,
    user=...,
    ...
)

def get_connection():
    return pool.connection()
```

---

### 9. 全局状态导致并发安全问题

**严重程度**：🟠 中危

**问题描述**：
多处使用全局内存存储，存在并发安全和扩展性问题：

| 全局变量 | 用途 | 问题 |
|---------|------|------|
| `current_session_user` | 当前用户 | 并发覆盖 |
| `trace_store` | Trace 追踪 | 内存泄漏、进程重启丢失 |
| `sessions` (飞书) | 飞书会话 | 内存泄漏、多实例不一致 |
| `SimpleCache` | 问答缓存 | 内存泄漏、多实例不共享 |

**涉及文件**：
- [chat.py](file:///workspace/med_agent_v1/chat.py#L28-L29) - trace_store
- [feishu_adapter.py](file:///workspace/med_agent_v1/feishu_adapter.py#L26-L26) - sessions
- [chat.py](file:///workspace/med_agent_v1/chat.py#L192-L208) - SimpleCache

**修复建议**：
- 使用 Redis 替代内存缓存和会话存储
- 用户身份使用 JWT，不存全局变量

---

### 10. 单文件过大，职责混乱

**严重程度**：🟠 中危

**问题描述**：
`chat.py` 超过 **1300 行**，一个文件承担了太多职责：

| 职责 | 行数估算 |
|------|---------|
| FastAPI 应用初始化 | ~50 |
| 日志配置 | ~70 |
| Pydantic 模型定义 | ~100 |
| 中间件（租户、监控） | ~80 |
| 全局组件初始化 | ~20 |
| 缓存逻辑 | ~50 |
| 核心问答逻辑 | ~300 |
| /ask 接口 | ~250 |
| /consult 接口 | ~200 |
| 审批接口 | ~100 |
| 文件上传/下载 | ~150 |
| 历史记录接口 | ~100 |
| Trace 接口 | ~80 |
| 报告预览 | ~150 |
| **总计** | **~1700** |

**涉及文件**：[chat.py](file:///workspace/med_agent_v1/chat.py)

**修复建议**：
按功能拆分模块：
```
app/
├── main.py              # 应用入口
├── api/
│   ├── __init__.py
│   ├── chat.py          # 问答接口
│   ├── approval.py      # 审批接口
│   ├── file.py          # 文件上传下载
│   ├── history.py       # 历史记录
│   └── health.py        # 健康检查/监控
├── services/
│   ├── chat_service.py  # 问答业务逻辑
│   ├── patient_service.py
│   └── approval_service.py
├── repositories/
│   ├── patient_repo.py  # 数据访问层
│   └── approval_repo.py
├── middleware/
│   ├── tenant.py
│   └── metrics.py
└── models/
    └── schemas.py       # Pydantic 模型
```

---

### 11. 大量重复代码

**严重程度**：🟠 中危

**问题描述**：
`/ask` 和 `/consult` 两个端点有大量重复逻辑：

| 重复逻辑 | 估算行数 |
|---------|---------|
| 身份声明处理 | ~40 |
| 身份查询处理 | ~40 |
| 患者操作拦截 | ~60 |
| 审批指令拦截 | ~80 |
| 超时控制 + 错误处理 | ~50 |
| 历史记录保存 | ~30 |
| **总计** | **~300 行** |

**涉及文件**：[chat.py](file:///workspace/med_agent_v1/chat.py#L426-L869)

**修复建议**：
提取公共逻辑到中间件或依赖注入函数：
```python
async def common_message_handler(question: str, session_id: str, ...):
    """处理身份声明、患者操作、审批拦截等公共逻辑"""
    ...
```

---

### 12. 硬编码路径

**严重程度**：🟠 中危

**问题描述**：
大量硬编码的本地路径，无法在其他环境运行。

```python
# app.py 第15行
VECTOR_DB_DIR = "/mnt/d/A_Study/Agent/Med_Agent/vector_db"
```

```yaml
# docker-compose.yml
volumes:
  - /mnt/d/A_Study/Agent/Med_Agent/vector_db:/app/vector_db
  - /mnt/d/A_Study/Agent/Med_Agent/reports:/app/reports
  - /mnt/d/A_Study/Agent/Med_Agent/ollama_models:/root/.ollama
```

**涉及文件**：
- [app.py](file:///workspace/med_agent_v1/app.py#L15-L15)
- [docker-compose.yml](file:///workspace/med_agent_v1/docker-compose.yml)

**修复建议**：
- 使用环境变量配置路径
- Docker volume 使用相对路径或命名卷

---

## 🟡 三、代码质量与可维护性问题（P2 - 优化）

### 13. .gitignore 忽略了所有 .md 文件

**严重程度**：🟡 低危

**问题描述**：
```gitignore
# .gitignore 第40-41行
README.md
*.md
```

所有 Markdown 文档都不会被提交，包括 README、设计文档等。这非常不合理。

**涉及文件**：[.gitignore](file:///workspace/med_agent_v1/.gitignore#L40-L41)

**修复建议**：删除这两行，文档应该提交到 Git。

---

### 14. .bak 文件提交到仓库

**严重程度**：🟡 低危

**问题描述**：
`tools/approval_tool.py.bak` 备份文件提交到了版本控制。

**涉及文件**：[approval_tool.py.bak](file:///workspace/med_agent_v1/tools/approval_tool.py.bak)

**修复建议**：
- 删除 .bak 文件
- 在 .gitignore 中添加 `*.bak`

---

### 15. requirements.txt 版本混乱

**严重程度**：🟡 低危

**问题**：
1. `python-docx` 出现了两次（第14行 `python-docx==1.1.2`，第17行 `python-docx>=0.8.11`）
2. 大部分依赖没有锁定版本号（只有 `prometheus-client==0.20.0` 锁定了）
3. 生产依赖和开发依赖混在一起（pytest 系列应该是 dev 依赖）

**涉及文件**：[requirements.txt](file:///workspace/med_agent_v1/requirements.txt)

**修复建议**：
- 分离 `requirements.txt` 和 `requirements-dev.txt`
- 全部锁定版本号
- 或使用 Poetry / Pipenv 管理依赖

---

### 16. 患者姓名提取逻辑重复

**严重程度**：🟡 低危

**问题描述**：
`planner.py` 中有两个几乎相同的函数，正则表达式也重复定义了多次：

- `_extract_patient_name_from_history`（从历史提取）
- `_extract_name_from_text`（从当前文本提取）

**涉及文件**：[planner.py](file:///workspace/med_agent_v1/agents/planner.py#L28-L60)

**修复建议**：提取公共函数，复用正则模式。

---

### 17. 日志级别使用不当

**严重程度**：🟡 低危

**问题示例**：
```python
# chat.py 第431行
logger.error(f"[ASK] 所有 headers: {request.headers}")
```

打印请求头用 `error` 级别不合适，应该用 `debug` 或 `info`。

还有大量 `logger.warning` 用来打普通信息。

**修复建议**：统一日志级别规范：
- `ERROR`：系统错误、异常
- `WARNING`：告警、潜在问题
- `INFO`：正常业务流程
- `DEBUG`：调试信息

---

### 18. SQL 注入风险（低）

**严重程度**：🟡 低危

**问题描述**：
`_init_db` 中使用字符串拼接执行 DDL 语句：

```python
# tools/patient_tool.py 第50-52行
cursor.execute(f"SHOW COLUMNS FROM patients LIKE '{field}'")
if not cursor.fetchone():
    cursor.execute(f"ALTER TABLE patients ADD COLUMN {field} {col_type}")
```

虽然 `field` 来自硬编码列表，风险较低，但这是不好的实践。

**涉及文件**：[patient_tool.py](file:///workspace/med_agent_v1/tools/patient_tool.py#L50-L52)

---

## 💡 四、改进建议优先级

### P0（立即修复，安全相关）

| 序号 | 问题 | 预计工作量 |
|------|------|-----------|
| 1 | 修复 crypto.py 中 logger 未定义的 bug | 5 分钟 |
| 2 | 修复路径遍历漏洞（报告下载/预览接口） | 30 分钟 |
| 3 | 强制要求 CRYPTO_KEY 环境变量，禁止自动生成 | 10 分钟 |
| 4 | 生产环境限制 CORS 来源 | 10 分钟 |
| 5 | 增加用户认证机制（替换全局变量） | 2-3 天 |

### P1（近期修复，架构相关）

| 序号 | 问题 | 预计工作量 |
|------|------|-----------|
| 6 | 引入数据库连接池 | 1 天 |
| 7 | 重构循环依赖，使用依赖注入传递 tenant/user | 2 天 |
| 8 | 拆分 chat.py 为多个模块 | 3-5 天 |
| 9 | 提取重复代码（/ask 和 /consult 的公共逻辑） | 1 天 |
| 10 | 使用 Redis 替代内存缓存和 trace 存储 | 2 天 |

### P2（中期优化，质量相关）

| 序号 | 问题 | 预计工作量 |
|------|------|-----------|
| 11 | 修复 .gitignore，恢复 .md 文件提交 | 10 分钟 |
| 12 | 清理 .bak 文件 | 5 分钟 |
| 13 | 整理 requirements.txt，锁定版本 | 1 小时 |
| 14 | 补充单元测试覆盖率 | 持续 |
| 15 | 增加接口权限校验 | 2-3 天 |

### P3（长期演进）

| 序号 | 问题 | 预计工作量 |
|------|------|-----------|
| 16 | 患者敏感字段全部加密存储 | 2-3 天 |
| 17 | 引入数据备份机制 | 1 天 |
| 18 | 增加限流防刷 | 1 天 |
| 19 | 完善审计日志 | 2 天 |
| 20 | 增加数据脱敏（日志、返回值） | 2 天 |

---

## 📊 问题统计

| 严重程度 | 数量 | 占比 |
|---------|------|------|
| 🔴 高危（P0） | 6 | 33% |
| 🟠 中危（P1） | 6 | 33% |
| 🟡 低危（P2） | 6 | 33% |
| **总计** | **18** | **100%** |

| 类别 | 数量 |
|------|------|
| 安全问题 | 6 |
| 架构/可靠性问题 | 6 |
| 代码质量问题 | 6 |

---

## ✅ 总结

### 亮点

1. **功能完整**：多Agent架构、工具编排、RAG检索、飞书集成、监控都有了
2. **多科室路由**：心内科、药剂科、全科分Agent处理，有聚合层
3. **混合检索**：向量检索 + BM25 + LLM重排序
4. **审计日志**：有审计表和日志记录
5. **监控指标**：Prometheus 指标暴露
6. **飞书集成**：长连接模式，体验好

### 主要风险

1. **安全债多**：医疗数据敏感，但安全措施不足
2. **架构债多**：单文件过大、循环依赖、全局状态
3. **可扩展性差**：内存存储、无连接池、硬编码路径

### 建议路线

**第一周**：修复所有 P0 安全问题
- crypto.py bug 修复
- 路径遍历漏洞修复
- CORS 限制
- CRYPTO_KEY 强制配置

**第二周**：解决 P1 架构问题（上半部分）
- 引入连接池
- 重构循环依赖
- 修复 .gitignore 和依赖

**第三-四周**：解决 P1 架构问题（下半部分）
- 拆分 chat.py
- 提取重复代码
- Redis 替换内存存储

---

*报告生成时间：2026-06-29*
